from django.apps import AppConfig


class PurchaseDashboardsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "purchase_dashboards"
    verbose_name = "Purchase — Control Dashboards"
